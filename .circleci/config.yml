# Scala CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/sample-config/ for more details
#
version: 2
jobs:
  merge:
    docker:
      - image: circleci/openjdk:8-jdk
    working_directory: ~/repo
    steps:
      - checkout
      - run: git config --global user.email "eric.zhouqi@gmail.com"
      - run: git pull origin another-demo
workflows:
  version: 2
  build_and_merge:
    jobs:
      - merge